# steep

A minimal Go package for building multi-screen Bubble Tea TUI applications.

## examples

An example multi-screen application can be found in `./examples/multiscreen`, and executed by running `go run examples/multiscreen`.
